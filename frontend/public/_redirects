# Brand stores use /s/shop?slug={brand} — do NOT add a /:slug catch-all.
# That rule hijacked every marketing route (/pos, /about, /login, …)
# into the storefront shell under @cloudflare/next-on-pages.
